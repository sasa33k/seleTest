package com.s3a.test.module;

public enum SeDoType {

	FetchingPage,getText,click,sendKeys,clear,selectByIndex,selectByVisibleText,selectByValue,isSelected,isEnabled,isDisplayed,noThing, imWait, alert;
}
